import React from 'react';
import { MainBtn } from './style';

const MainButton = ({name}) => {
    return (
        <>
            <MainBtn>{name}</MainBtn>
        </>
    );
}

export default MainButton;
